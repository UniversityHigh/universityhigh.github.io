block navbar
nav.navbar.navbar-default.navbar-fixed-top(role = "navigation")
	.container
		.navbar-header
			//- Hamburger menu
			button.navbar-toggle.collapsed.pull-left(data-toggle = "collapse", data-target="#navbar-links")
				span.sr-only Toggle Navigation
				//- Repeated three times because each makes up a line in the hamburger
				span.icon-bar
				span.icon-bar
				span.icon-bar
			a.navbar-brand(href = "/") UHSSE
		div.collapse.navbar-collapse#navbar-links
			ul.nav.navbar-nav.navbar-right
				button.btn.btn-danger.navbar-btn(type = "button") STUDENTS
				each data, name in navbar
					if data instanceof Array
						//- Dropdown required
						li.dropdown
							a.dropdown-toggle(href = data[0], data-toggle = "dropdown") #{name} #[span.caret]
							ul.dropdown-menu
								each subLink, subName in data[1]
									li
										a.nav-link(href = data[0] + "#" + subLink)= subName
					else
						li
							a.nav-link(href = data)= name